---
trigger: always_on
---

For this project pipenv is used. So to run any python files that is belong to this project must use "python3 -m pipenv run python script.py" convetion to avoid problems.
If a package is to be installed instal it to pipenv.
